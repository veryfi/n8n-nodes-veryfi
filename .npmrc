# @n8n/node-cli's bundled eslint config does runtime requires for its peers
# (@eslint/js, @typescript-eslint/*, etc.) that pnpm's strict resolver can't
# resolve when the CLI lives under .pnpm/. Use a flat node_modules layout
# (npm/yarn-style) so those requires resolve.
node-linker=hoisted
